import React from 'react';

/**
 * Renders DocumentContent component
 *
 * @param  {object} content
 * @return {object} Document content
 */
const DocumentContent = ({ content }) => (
  <span>
    {content}
  </span>
  );

export default DocumentContent;
